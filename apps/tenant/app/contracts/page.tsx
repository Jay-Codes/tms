'use client';

import { Placeholder } from '../../components/Placeholder';

export default function ContractsPage() {
  return (
    <Placeholder
      title="Contracts"
      phase={4}
      what="Contract templates, the signing flow, countersigning and the printable contract document."
    />
  );
}
