'use client';

import { Placeholder } from '../../components/Placeholder';

export default function PropertiesPage() {
  return (
    <Placeholder
      title="Properties"
      phase={2}
      what="Properties and units, custom unit names, prices per N days, the vacancy board and printable QR codes."
    />
  );
}
