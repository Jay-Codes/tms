'use client';

import { Placeholder } from '../../components/Placeholder';

export default function PaymentsPage() {
  return (
    <Placeholder
      title="Payments"
      phase={5}
      what="Payment schedules, recording offline payments (cash, bank, mobile money), reversals and receipts."
    />
  );
}
