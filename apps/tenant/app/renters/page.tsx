'use client';

import { Placeholder } from '../../components/Placeholder';

export default function RentersPage() {
  return (
    <Placeholder
      title="Renters"
      phase={3}
      what="Renter directory, KYC details (NIDA, next of kin) and pending link requests to approve or reject."
    />
  );
}
