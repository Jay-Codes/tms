'use client';

export default function Home() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        background: '#0ea5e9',
        color: 'white',
      }}
    >
      <h1>Hello, End User 👋</h1>
      <p>TMS end-user app — dev preview</p>
    </main>
  );
}
