// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: payment_schedules.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createPaymentSchedule = `-- name: CreatePaymentSchedule :one

INSERT INTO payment_schedules (org_id, contract_id, period_start, period_end, due_date, amount)
VALUES (
    $1, $2, $3,
    $4, $5, $6
)
RETURNING id, org_id, contract_id, period_start, period_end, due_date, amount, status, created_at, updated_at, deleted_at, paid_amount
`

type CreatePaymentScheduleParams struct {
	OrgID       pgtype.UUID `json:"org_id"`
	ContractID  pgtype.UUID `json:"contract_id"`
	PeriodStart pgtype.Date `json:"period_start"`
	PeriodEnd   pgtype.Date `json:"period_end"`
	DueDate     pgtype.Date `json:"due_date"`
	Amount      int64       `json:"amount"`
}

// payment_schedules are written in one go at activation: every row of the term
// at the contract's cadence, amounts prorated from the snapshotted rent
// (SPEC §4). Phase 5 records payments against them; Phase 4 only reads.
func (q *Queries) CreatePaymentSchedule(ctx context.Context, arg CreatePaymentScheduleParams) (PaymentSchedule, error) {
	row := q.db.QueryRow(ctx, createPaymentSchedule,
		arg.OrgID,
		arg.ContractID,
		arg.PeriodStart,
		arg.PeriodEnd,
		arg.DueDate,
		arg.Amount,
	)
	var i PaymentSchedule
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.ContractID,
		&i.PeriodStart,
		&i.PeriodEnd,
		&i.DueDate,
		&i.Amount,
		&i.Status,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
		&i.PaidAmount,
	)
	return i, err
}

const listSchedulesForContract = `-- name: ListSchedulesForContract :many
SELECT id, org_id, contract_id, period_start, period_end, due_date, amount, status, created_at, updated_at, deleted_at, paid_amount FROM payment_schedules
WHERE org_id = $1 AND contract_id = $2 AND deleted_at IS NULL
ORDER BY period_start, id
`

type ListSchedulesForContractParams struct {
	OrgID      pgtype.UUID `json:"org_id"`
	ContractID pgtype.UUID `json:"contract_id"`
}

func (q *Queries) ListSchedulesForContract(ctx context.Context, arg ListSchedulesForContractParams) ([]PaymentSchedule, error) {
	rows, err := q.db.Query(ctx, listSchedulesForContract, arg.OrgID, arg.ContractID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PaymentSchedule{}
	for rows.Next() {
		var i PaymentSchedule
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ContractID,
			&i.PeriodStart,
			&i.PeriodEnd,
			&i.DueDate,
			&i.Amount,
			&i.Status,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
			&i.PaidAmount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSchedulesForRenter = `-- name: ListSchedulesForRenter :many
SELECT s.id, s.org_id, s.contract_id, s.period_start, s.period_end, s.due_date,
       s.amount, s.status, s.paid_amount, s.created_at,
       c.status AS contract_status,
       u.name AS unit_name, p.name AS property_name, o.name AS org_name
FROM payment_schedules s
JOIN contracts c  ON c.id = s.contract_id AND c.org_id = s.org_id
JOIN units u      ON u.id = c.unit_id AND u.org_id = c.org_id
JOIN properties p ON p.id = u.property_id AND p.org_id = c.org_id
JOIN orgs o       ON o.id = c.org_id
WHERE c.renter_user_id = $1
  AND s.deleted_at IS NULL AND c.deleted_at IS NULL
ORDER BY s.due_date, s.period_start, s.id
`

type ListSchedulesForRenterRow struct {
	ID             pgtype.UUID        `json:"id"`
	OrgID          pgtype.UUID        `json:"org_id"`
	ContractID     pgtype.UUID        `json:"contract_id"`
	PeriodStart    pgtype.Date        `json:"period_start"`
	PeriodEnd      pgtype.Date        `json:"period_end"`
	DueDate        pgtype.Date        `json:"due_date"`
	Amount         int64              `json:"amount"`
	Status         string             `json:"status"`
	PaidAmount     int64              `json:"paid_amount"`
	CreatedAt      pgtype.Timestamptz `json:"created_at"`
	ContractStatus string             `json:"contract_status"`
	UnitName       string             `json:"unit_name"`
	PropertyName   string             `json:"property_name"`
	OrgName        string             `json:"org_name"`
}

// ListSchedulesForRenter serves GET /me/schedules across every org the renter
// rents from, so it is scoped by the renter rather than by an org.
// guard-exempt: the renter's own schedules, keyed by contracts.renter_user_id across orgs.
func (q *Queries) ListSchedulesForRenter(ctx context.Context, renterUserID pgtype.UUID) ([]ListSchedulesForRenterRow, error) {
	rows, err := q.db.Query(ctx, listSchedulesForRenter, renterUserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListSchedulesForRenterRow{}
	for rows.Next() {
		var i ListSchedulesForRenterRow
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.ContractID,
			&i.PeriodStart,
			&i.PeriodEnd,
			&i.DueDate,
			&i.Amount,
			&i.Status,
			&i.PaidAmount,
			&i.CreatedAt,
			&i.ContractStatus,
			&i.UnitName,
			&i.PropertyName,
			&i.OrgName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const waiveSchedulesAfter = `-- name: WaiveSchedulesAfter :many
UPDATE payment_schedules
SET status = 'waived'
WHERE org_id = $1 AND contract_id = $2
  AND status IN ('pending', 'overdue', 'partial')
  AND period_start > $3
  AND deleted_at IS NULL
RETURNING id
`

type WaiveSchedulesAfterParams struct {
	OrgID         pgtype.UUID `json:"org_id"`
	ContractID    pgtype.UUID `json:"contract_id"`
	EffectiveDate pgtype.Date `json:"effective_date"`
}

// WaiveSchedulesAfter cancels what a terminated contract will never be billed
// for: rows whose period starts after the termination takes effect. Rows the
// renter has already lived through stand, paid or not (FLOWS 6.5).
func (q *Queries) WaiveSchedulesAfter(ctx context.Context, arg WaiveSchedulesAfterParams) ([]pgtype.UUID, error) {
	rows, err := q.db.Query(ctx, waiveSchedulesAfter, arg.OrgID, arg.ContractID, arg.EffectiveDate)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []pgtype.UUID{}
	for rows.Next() {
		var id pgtype.UUID
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
