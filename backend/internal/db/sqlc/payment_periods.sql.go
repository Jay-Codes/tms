// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: payment_periods.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createPaymentPeriod = `-- name: CreatePaymentPeriod :one
INSERT INTO payment_periods (org_id, label, days, is_recommended, sort_order)
VALUES (
    $1,
    $2,
    $3,
    $4,
    $5
)
RETURNING id, org_id, label, days, is_recommended, sort_order, active, created_at, updated_at, deleted_at
`

type CreatePaymentPeriodParams struct {
	OrgID         pgtype.UUID `json:"org_id"`
	Label         string      `json:"label"`
	Days          int32       `json:"days"`
	IsRecommended bool        `json:"is_recommended"`
	SortOrder     int32       `json:"sort_order"`
}

func (q *Queries) CreatePaymentPeriod(ctx context.Context, arg CreatePaymentPeriodParams) (PaymentPeriod, error) {
	row := q.db.QueryRow(ctx, createPaymentPeriod,
		arg.OrgID,
		arg.Label,
		arg.Days,
		arg.IsRecommended,
		arg.SortOrder,
	)
	var i PaymentPeriod
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.Label,
		&i.Days,
		&i.IsRecommended,
		&i.SortOrder,
		&i.Active,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const listPaymentPeriods = `-- name: ListPaymentPeriods :many
SELECT id, org_id, label, days, is_recommended, sort_order, active, created_at, updated_at, deleted_at FROM payment_periods
WHERE org_id = $1 AND deleted_at IS NULL
ORDER BY sort_order ASC, days ASC
`

func (q *Queries) ListPaymentPeriods(ctx context.Context, orgID pgtype.UUID) ([]PaymentPeriod, error) {
	rows, err := q.db.Query(ctx, listPaymentPeriods, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PaymentPeriod{}
	for rows.Next() {
		var i PaymentPeriod
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.Label,
			&i.Days,
			&i.IsRecommended,
			&i.SortOrder,
			&i.Active,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
